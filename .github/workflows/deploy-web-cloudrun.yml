name: Deploy Web (Cloud Run)

on:
  push:
    branches: [ main ]
    paths:
      - 'sharenest/apps/web/**'
      - 'sharenest/packages/**'
      - 'sharenest/pnpm-workspace.yaml'
      - 'sharenest/pnpm-lock.yaml'
      - 'sharenest/package.json'
  workflow_dispatch:

concurrency:
  group: web-deploy-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: sharenestjapan
  REGION: asia-northeast1
  REPOSITORY: sharenestjapan
  SERVICE_NAME: sharenest-web
  IMAGE_NAME: web
  REGISTRY_HOST: asia-northeast1-docker.pkg.dev
  IMAGE_URI: asia-northeast1-docker.pkg.dev/sharenestjapan/sharenestjapan/web

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          # 萓・ projects/828069558990/locations/global/workloadIdentityPools/github-actions/providers/github
          workload_identity_provider: ${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          project_id: ${{ env.PROJECT_ID }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker ${{ env.REGISTRY_HOST }} --quiet

      - name: Build Docker image (web)
        run: |
          
            sharenest

      - name: Push Docker image
        run: |
          docker push ${{ env.IMAGE_URI }}:${{ github.sha }}
          docker push ${{ env.IMAGE_URI }}:latest

      - name: Deploy to Cloud Run (web)
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --project=${{ env.PROJECT_ID }} \
            --region=${{ env.REGION }} \
            --image=${{ env.IMAGE_URI }}:${{ github.sha }} \
            --platform=managed \
            --allow-unauthenticated \
            --port=8080 \
            --concurrency=80 \
            --min-instances=0 \
            --memory=512Mi \
            --set-env-vars=NODE_ENV=production,NEXT_PUBLIC_SUPABASE_URL='${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}',NEXT_PUBLIC_SUPABASE_ANON_KEY='${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}',NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY='${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}',NEXT_PUBLIC_SITE_URL='${{ secrets.SITE_URL }}',SITE_URL='${{ secrets.SITE_URL }}'

      - name: Show service URL
        run: |
          gcloud run services describe ${{ env.SERVICE_NAME }} --region=${{ env.REGION }} --format='value(status.url)'

